---
layout: post
title: Spring学习笔记
header-img: img/in-post/head/7.jpg
header-style: text
catalog: true
tags:
  - Java基础
  - Spring
---
本篇blog用于记录Spring学习过程，浅浅研究一下Spring的架构。
### 1、IoC、DI和AOP
**Ioc(Inversion of Control)控制反转**，即将程序中创建bean的权利交给第三方。

通俗来讲，IoC指的是将对象的创建、管理和销毁的控制权交给Spring容器，而不是在应用程序代码中手动创建这些对象。这种控制反转的思想使得应用程序更加灵活、可维护，并降低了对象之间的耦合度

也就是说，程序员不用再管理bean的创建，在需要更替bean实例时也不需要侵入式修改源码，只需要让第三方托管新的bean实例即可。

**DI(Dependency Injection)依赖注入**，即在程序需要的时候，由第三方框架来注入管理的bean。

与IoC密切相关的是DI，即依赖注入。通过依赖注入，Spring容器可以将对象之间的依赖关系动态地注入到对象中，从而避免了硬编码的依赖关系。这种松耦合的设计使得应用程序更易于测试、维护和扩展，也使得代码更具有可读性和可维护性。

这个相信入门java web的小伙伴都不陌生，在Controller层注入ServiceImpl，或者Service层注入MapperImpl都是家常便饭了。

**AOP(Aspect Oriented Programming)**，面向切面编程。

这个概念比较绕，实际上就是对业务进行一些增强工作，比如拦截一个类的方法，随后在这个方法的前后插入一些日志工作，最后返回结果。

AOP的核心概念是切面（Aspect），切面模块化了横切关注点，并定义了在何时、何地以及如何应用这些关注点。Spring框架通过AOP提供了对方法拦截、异常处理、事务管理等功能的支持，使得开发人员能够更加方便地实现横切关注点的处理。

可以不负责任的说，IoC、DI和AOP就是Spring框架重要的核心特性。

### 2、BeanFactory和ApplicationContext
接下来的部分聊一聊Spring框架中两个重要类，**BeanFactory**和**ApplicaitonContext**

BeanFactory是Spring早期的接口，称作是Spring的Bean工厂，而ApplicationContext则是后期更为高级的接口，称之为Spring容器。

ApplicationContext在BeanFactory基础上对功能进行了拓展，例如：监听功能、国际化功能、XML文件读取等，BeanFactory的API更接近底层，而ApplicationContext的API则是对这些底层API的封装。

Bean创建的主要功能都在BeanFactory中，而ApplicationContext不仅继承了BeanFactory，而且在其内部还维护了一个BeanFactory的引用，所以它们之间不仅仅是is-a的关系，还是has-a的关系。

而还有一点区别是，BeanFactory中的Bean只有使用时才会被创建(懒加载)，而ApplicationContext则是加载配置文件后，便直接实例化并初始化Bean。

### 3、基于XML方式Bean的配置
虽然目前主流开发方式都是注解配置，但是我认为了解XML方式配置Bean是有必要的。

这能让我们更加清楚了解Spring组件的一些操作流程，并且说不定以后工作还要维护老项目不是？

| 配置项              | 说明                                                               | 示例                                                                 |
|---------------------|--------------------------------------------------------------------|----------------------------------------------------------------------|
| `id`               | Bean 的唯一标识，用于在容器中引用。                                  | `<bean id="myBean" class="com.example.Bean"/>`                      |
| `name`             | Bean 的别名，可以有多个，用逗号、分号或空格分隔。                   | `<bean name="beanAlias1, beanAlias2" class="com.example.Bean"/>`     |
| `class`            | 定义创建 Bean 实例的全限定类名。                                     | `<bean id="myBean" class="com.example.Bean"/>`                      |
| `scope`            | Bean 的作用域：`singleton`、`prototype`、`request`、`session` 等。   | `<bean id="myBean" scope="prototype" class="com.example.Bean"/>`     |
| `lazy-init`        | 是否延迟初始化，`true` 表示延迟加载，`false` 表示非延迟加载（默认）。| `<bean id="myBean" lazy-init="true" class="com.example.Bean"/>`      |
| `init-method`      | Bean 初始化时调用的方法。                                            | `<bean id="myBean" init-method="init" class="com.example.Bean"/>`    |
| `destroy-method`   | Bean 销毁时调用的方法。                                              | `<bean id="myBean" destroy-method="cleanup" class="com.example.Bean"/>` |
| `autowire`         | 自动装配模式：`no`（默认）、`byName`、`byType`、`constructor`。      | `<bean id="myBean" autowire="byName" class="com.example.Bean"/>`     |
| `depends-on`       | 指定 Bean 的依赖，确保依赖 Bean 先初始化。                          | `<bean id="myBean" depends-on="otherBean" class="com.example.Bean"/>`|
| `constructor-arg`  | 通过构造方法为 Bean 注入依赖。                                       | `<bean id="myBean" class="com.example.Bean"><constructor-arg value="123"/></bean>` |
| `property`         | 通过 Setter 方法为 Bean 注入依赖。                                   | `<bean id="myBean" class="com.example.Bean"><property name="name" value="test"/></bean>` |

其中我省略了和工厂有关的一部分内容，因为这里稍微有些复杂，需要分为三种情况来讨论。

#### 补充：Bean配置之工厂

##### 一、静态工厂

静态工厂是通过类的**静态方法**来返回Bean实例。它的优点是无需创建工厂类的对象，我们直接通过类名调用静态方法。

配置示例：
```xml
<bean id="myBean" class="com.example.StaticFactory" factory-method="createInstance" />
```
工厂类示例:
```java
public class StaticFactory {
    public static MyBean createInstance() {
        return new MyBean();
    }
}
```
通过静态方法创建 Bean，无需工厂类实例，适用于提供一个简单、直接的静态方法的场景。

##### 二、实例工厂

实例工厂是通过工厂类的实例方法来返回Bean实例。

配置示例：
```xml
<bean id="myFactory" class="com.example.InstanceFactory" />
<bean id="myBean" factory-bean="myFactory" factory-method="createInstance" />
```
工厂类示例：
```java
public class InstanceFactory {
    public MyBean createInstance() {
        return new MyBean();
    }
}
```

可以看见，首先需要将工厂的bean交给Spring容器管理，然后还需要声明一个bean，并且在factory-bean标签内标明工厂bean的beanname。

这里补充一个小知识，bean name在默认情况下是bean id，而在没有配置id的情况下，name会是myClass(假设类名为MyClass)。

总结，实例广场需要先创建工厂类的Bean，再通过工厂类的实例方法来创建目标Bean，适用于复杂的工厂逻辑，或者工厂类需要维护状态的场景。

##### 三、实现FactoryBean接口

这种方式是通过实现Spring提供的**org.springframework.beans.factory.FactoryBean**接口来创建 Bean。

FactoryBean 接口提供三个方法：

Object getObject()：返回创建的 Bean 实例。

Class<?> getObjectType()：返回创建 Bean 的类型。

boolean isSingleton()：返回 Bean 是否为单例。

配置示例：
```xml
<bean id="myBean" class="com.example.MyFactoryBean" />
```

工厂示例：
```java
public class MyFactoryBean implements FactoryBean<MyBean> {
    @Override
    public MyBean getObject() {
        return new MyBean();
    }

    @Override
    public Class<?> getObjectType() {
        return MyBean.class;
    }

    @Override
    public boolean isSingleton() {
        return true; // 单例
    }
}
```
适合复杂的 Bean 创建场景，如动态生成 Bean 或提供代理对象。

值得注意的是，FactoryBean本身会作为一个特殊的Bean存在，返回的对象是getObject()方法的返回值。

除了这些外，关于init-method标签还有个执行顺序的问题，我们留在后面学完BeanFactory和Bean的后处理器后再说。

#### 补充：Bean配置之注入

在这部分补充内容里，我想来聊一聊关于使用XML进行注入时的一些方式。

To be continued...
